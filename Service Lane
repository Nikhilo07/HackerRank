#include <bits/stdc++.h>
using namespace std;
int main() {
    long long int n,t;
    cin>>n>>t;
    long long int a[n];
    for(long long int i=0;i<n;i++)
    {
        cin>>a[i];
    }
   long long int s,e,sum;
    while(t--)
    {
        cin>>s>>e;
      cout<< *min_element(a+s, a+e+1)<<endl;
               


    }
}
